name: Deploy after push
on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
jobs:
  build-docs:
    uses: envicutor/docs-workflows/.github/workflows/push.yml@main
    with:
      target: spec
    secrets:
      pat: ${{ secrets.pat }}
